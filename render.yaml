services:
  - type: web
    name: royal-enfield-vishwanath-enterprises
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
